baseURL = 'https://example.org/'
languageCode = 'en-US'
title = 'Hugo Forum Topic #54486'
disableKinds = ['rss','sitemap','taxonomy','term']

[module]
[module.hugoVersion]
min = '0.146.7'

[params.render_hooks.link]
errorLevel = 'warning' # ignore (default), warning, or error (fails the build)
