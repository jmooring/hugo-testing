baseURL = 'https://example.org/'
languageCode = 'en-US'
title = 'Hugo GitHub Issue #13186'

[markup.goldmark.renderHooks.link]
enableDefault = true # this must be enabled for infinite scrolling to work properly

[markup.goldmark.renderHooks.image]
enableDefault = true # this must be enabled for infinite scrolling to work properly

[outputs]
page = ['html','json']
