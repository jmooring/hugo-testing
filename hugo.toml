baseURL = 'https://example.org/'
languageCode = 'en-US'
title = 'Hugo GitHub Issue #13443'

# -----------------------------------------------------------------------------
# Related
# -----------------------------------------------------------------------------

[related]
  includeNewer = true
  threshold = 80
  toLower = false
[[related.indices]]
  name = 'Keywords' # seems to be case-insensitive
  weight = 80

# -----------------------------------------------------------------------------
# Menus
# -----------------------------------------------------------------------------

[[menus.main]]
name = 'Home'
pageRef = '/'
weight = 10

[[menus.main]]
name = 'Books'
pageRef = '/books'
weight = 20

[[menus.main]]
name = 'Posts'
pageRef = '/posts'
weight = 30
