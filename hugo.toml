disableKinds = ['page', 'rss', 'section', 'sitemap', 'taxonomy', 'term']

[security.exec]
  allow = ['^(dart-)?sass(-embedded)?$', '^go$', '^git$', '^node$', '^postcss$', '^tailwindcss$', '^rst2html$','^asciidoctor$']

[markup.rst]
  syntaxHighlight = 'short'
