defaultContentLanguageInSubdir = true
disableKinds                   = ['page', 'section', 'rss', 'sitemap', 'taxonomy', 'term']

[languages.en]
[languages.de]

[outputFormats.foo]
  baseName  = 'foo'
  isHTML    = true
  mediaType = 'text/html'

[outputs]
  home = ['html', 'foo']
