+++
title = 'Home'
date = 2023-01-01T00:00:00-00:00
draft = false
details = '@@URL@@'
description = "@@DESCRIPTION@@"
+++

linenos=table,noClasses=true

```go {hl_lines=["2"],linenos=table,noClasses=true}
a := 1
b := 2
c := a + b 
fmt.Println(c)
if c == 0 {
  fmt.Println("wrong")
}
```

linenos=table,noClasses=false

```go {hl_lines=["2"],linenos=table,noClasses=false}
a := 1
b := 2
c := a + b 
fmt.Println(c)
if c == 0 {
  fmt.Println("wrong")
}
```

linenos=inline,noClasses=true

```go {hl_lines=["2"],linenos=inline,noClasses=true}
a := 1
b := 2
c := a + b 
fmt.Println(c)
if c == 0 {
  fmt.Println("wrong")
}
```

linenos=inline,noClasses=false

```go {hl_lines=["2"],linenos=inline,noClasses=false}
a := 1
b := 2
c := a + b 
fmt.Println(c)
if c == 0 {
  fmt.Println("wrong")
}
```
