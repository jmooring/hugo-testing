+++
title = 'Books'
date = 2025-09-28T06:50:52-07:00
draft = false
+++

The content on this page is generated by this template code:

```go-html-template
{{ range .Pages.ByTitle }}
  <h2><a href="{{ .RelPermalink }}">{{ .LinkTitle }}</a></h2>
  {{ .Content }}
{{ end }}
```

The uniqueness of footnote links across pages is guaranteed by the addition of a unique prefix to each footnote ID, thus preventing potential collisions. This functionality was integrated into Hugo as of version 0.151.0 and can be activated via the site configuration:

```toml
[markup.goldmark.extensions.footnote]
  enableAutoIDPrefix = true
```
