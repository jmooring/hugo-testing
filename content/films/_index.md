+++
title = 'Films'
date = 2025-09-28T06:51:01-07:00
draft = false
+++

The content on this page is generated by this shortcode:

```go-html-template
{{ $section := or (.Get "section") "/" }}
{{ with site.GetPage $section }}
  {{ range .Pages.ByTitle }}
## [{{ .LinkTitle }}]({{ .RelPermalink }}){{/* Do not indent. */}}
{{ .RenderShortcodes }}{{/* Do not indent. */}}
  {{ end }}
{{ end }}
```

The shortcode is called from the section page like this:

```text
{{%/* list section=/films */%}}
```

This method places all footnotes at the bottom of the page. It requires that **each footnote reference within your page collection be unique**, regardless of the value set for `markup.goldmark.extensions.footnote.enableAutoIDPrefix` in your site configuration.

{{% list section=/films %}}
