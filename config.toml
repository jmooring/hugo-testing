baseURL = 'https://example.org/'
languageCode = 'en-US'
title = 'Hugo Forum Topic #48828'

[[menus.main]]
name = 'Home'
url = '/'       # Using url in order to test with v0.85.0 and earlier. Use pageref with newer versions.
weight = 10

[[menus.main]]
name = 'Posts'
url = '/posts'  # Using url in order to test with v0.85.0 and earlier. Use pageref with newer versions.
weight = 20

[[menus.main]]
name = 'Tags'
url = '/tags'   # Using url in order to test with v0.85.0 and earlier. Use pageref with newer versions.
weight = 30

[frontmatter]
  date = ['date', 'publishdate', 'pubdate', 'published', 'lastmod', 'modified']
  expiryDate = ['expirydate', 'unpublishdate']
  lastmod = [':git', 'lastmod', 'modified', 'date', 'publishdate', 'pubdate', 'published']
  publishDate = ['publishdate', 'pubdate', 'published', 'date']
